public class DuckExam {
    public static void main(String[] args) {
        Duck duck = new Duck();
        duck.sing();
        duck.fly();

        // Bird b = new Bird(); 추상 클래스는 인스턴스(객체)를 생성할 수 없다.
    }
}
